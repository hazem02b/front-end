# SQLite
DATABASE_URL=sqlite:///./dev_flask.db

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=15
REFRESH_TOKEN_EXPIRES_DAYS=7

# Email Configuration - Brevo (Recommended for production)
# Create free account at https://www.brevo.com (300 emails/day free)
# Get SMTP credentials: Settings > SMTP & API > Create SMTP Key
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_USER=your-brevo-email@example.com
EMAIL_PASSWORD=your-brevo-smtp-key-here
EMAIL_FROM=Forstek <noreply@forstek.tn>

# App
APP_HOST=127.0.0.1
APP_PORT=5000
